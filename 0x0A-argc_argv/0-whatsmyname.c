#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @argc: length of input array
 * @argv: input array
 * Return: Always 0 (success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
